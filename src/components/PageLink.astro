---
const {url, name} = Astro.props;
const nName:string = name || "";
const actualName:string = nName.length < 2 ? nName.toUpperCase() : nName.charAt(0).toUpperCase() + nName.slice(1);
---

<a href={url}>
    <li class="h-10 content-center ">
        {actualName}
    </li>
</a>